#include <stdio.h>
#define PI 3.141593

// "Assignment1 written by Jonathan Taylor"
/*  Project 1 -- A program that uses a loop that outputs a table of 
    angles in degrees and radians up to 360 degrees
    in increments of 10 degrees.
*/

int main()
{
    int degree = 0;
    double radians;

    printf("Degrees to Radians \n");

    for(degree=0;degree<=360;degree=degree+10)
    {
        radians=degree*PI/180;
        printf("%6i %9.6f \n", degree, radians);
    }

    return 0;


}

